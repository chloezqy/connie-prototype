# dependencies
node_modules

# build output
dist
dist-ssr

# local env files (real keys live here — never commit)
.env
.env.local
.env.*.local

# CR proprietary data (uploaded into Langflow, never committed)
backend-data/

# editor / OS
.DS_Store
*.log
npm-debug.log*

# compiled config artifacts (Vite reads vite.config.ts directly)
vite.config.js
vite.config.d.ts
*.tsbuildinfo
